/***********************************************
 * read_line: reads line into str. If line is  *
 *            longer than n, truncates the     *
 *            following characters. Returns    *
 *            the length of the line read.     *
 ***********************************************/
int read_line(char *str, int n);
