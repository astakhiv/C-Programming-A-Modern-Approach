/************************************************************
 * duplicate: Copies string str by allocating a block for a *
 *            duplicate. If malloc fails, returns null      *
 *            pointer. Otherwise, utilizes strcpy and       *
 *            returns pointer to the duplicated string      *
 ************************************************************/
char * duplicate(char *str);
